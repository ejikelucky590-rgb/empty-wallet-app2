name: empty_wallet_app2
description: Dove Music - African Creative Super-App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  supabase_flutter: ^2.0.2
  file_picker: ^8.0.0
  video_player: ^2.8.1
  just_audio: ^0.9.36
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  go_router: ^14.2.0
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  go_router: ^14.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
